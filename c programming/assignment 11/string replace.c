//2. WAP Replace all Occurrences of ‘a’ with $ in a String
#include<stdio.h>
#include<stdio.h>
void main()
{
	char str[20];
	int i,found=0;
	printf("Enter a replace");
	printf("Enter all Occurrences of ‘a’ with ");
	scanf("%d",str);
	for(i=0;i<strlen(str);i++)
	{
	
		found=0;
		break;
	}
printf("Replace all Occurrences of ‘a’ with $");
}
